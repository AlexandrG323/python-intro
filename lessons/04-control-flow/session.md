# Лог занятия

| Дата | Что разобрали | Упражнения | Слабые места |
| --- | --- | --- | --- |
| 2026-09-23 | Отступ как блок; `elif`; `while` вместо C-`for`; `range` как полуинтервал; `match` без проваливания; неявный `None` в конце функции. | `control_flow.py`, 5 тестов зелёные | Заголовок `for (int i…)` и `++`/`--` из C++; тень имён `list` и `sum`; `first_even` без явного `return None` и без `for`/`else` |
