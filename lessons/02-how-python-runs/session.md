# Лог занятия

| Дата | Что разобрали | Упражнения | Слабые места |
| --- | --- | --- | --- |
| 2026-09-18 | Цепочка `.py` → байткод → CPython; import один раз на процесс; `__name__ == "__main__"` vs библиотека; venv / `uv run` как изоляция, не «свежий python3». | `greeter.py`, `app.py`, `run_mode.py`, 4 теста зелёные | Байткод путали с машинным кодом; два процесса vs два импорта; кэш модуля путали с кэшем результата функции |
