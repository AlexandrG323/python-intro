# Лог занятия

| Дата | Что разобрали | Упражнения | Слабые места |
| --- | --- | --- | --- |
| 2026-09-21 | Имя как ярлык; `is` vs `==`; `None` через `is`; shallow copy vs второй ярлык; мутация списка на месте; `int` неизменяемый, `+=` перевешивает имя. Hints не проверяет интерпретатор. | `names_and_copies.py`, 6 тестов зелёные | `mutate_last` возвращает список при контракте `-> None`; hints путали с «сломается в рантайме» — может и не сломаться |
