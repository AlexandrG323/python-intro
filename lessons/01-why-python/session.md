# Лог занятия

| Дата | Что разобрали | Упражнения | Слабые места |
| --- | --- | --- | --- |
| 2026-09-14 | Зачем Python: короткий цикл «написал → увидел», type hints не компилируют, выбор языка как компромисс. Таблица правил и первое совпадение (`any`, `in`, приоритет железа). | `pick_language.py`, 11 тестов зелёные | `any` без проверки элемента; разные имена в генераторе (`kword` / `word`); `.lower()` и текст `ValueError` с нескольких попыток |
