# Лог занятия

| Дата | Что разобрали | Упражнения | Слабые места |
| --- | --- | --- | --- |
| 2026-09-25 | list / tuple / dict / set; срез как полуинтервал; ключ dict только хешируемый; `sorted(..., key=)` с двумя полями (счёт по убыванию, слово по алфавиту). | `collections_practice.py`, 6 тестов зелёные | `key=` передали в `dict`, и он стал полем; `list[obj]` приняли за вызов `list(obj)`; пустой список через `list[int]()`; в `total_by_year` индексы вместо распаковки и старт суммы с `int` |
